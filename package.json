{
  "name": "115master-fusion-monorepo",
  "private": true,
  "packageManager": "pnpm@9.15.9",
  "description": "115Master Fusion monorepo",
  "author": "cbingb666, daoran9",
  "license": "MIT",
  "homepage": "https://github.com/daoran9/115master",
  "repository": {
    "type": "git",
    "url": "https://github.com/daoran9/115master.git"
  },
  "bugs": {
    "url": "https://github.com/daoran9/115master/issues"
  },
  "engines": {
    "node": ">=20.12"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "type-check": "turbo run type-check",
    "test": "turbo run test",
    "test:watch": "turbo run test:watch",
    "test:ui": "turbo run test:ui",
    "test:coverage": "turbo run test:coverage",
    "lint": "turbo run lint",
    "lint:fix": "turbo run lint:fix",
    "lint:inspector": "pnpm dlx @eslint/config-inspector",
    "analyze": "turbo run analyze",
    "storybook": "turbo run storybook",
    "storybook:ui": "turbo run storybook --filter=@115master/ui",
    "storybook:monkey": "turbo run storybook --filter=@115master/monkey",
    "build-storybook": "turbo run build-storybook",
    "test:visual": "playwright test --config tests/visual/playwright.config.ts",
    "test:visual:update": "playwright test --config tests/visual/playwright.config.ts --update-snapshots",
    "test:e2e": "pnpm --filter @115master/monkey run test:e2e",
    "test:e2e:run": "pnpm --filter @115master/monkey run test:e2e:run",
    "verify": "pnpm type-check && pnpm test && pnpm test:e2e && pnpm build-storybook && pnpm test:visual",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "changeset publish",
    "preinstall": "npx -y only-allow pnpm",
    "clean:cache": "pnpm --include-workspace-root -r exec rm -rf .turbo .rollup.cache node_modules/.vite node_modules/.tmp",
    "clean": "pnpm -r --if-present run clean && pnpm clean:cache && rm -rf node_modules"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.5.2",
    "@changesets/cli": "^2.29.8",
    "@playwright/test": "^1.57.0",
    "cross-env": "^10.1.0",
    "eslint": "^9.39.2",
    "simple-git-hooks": "^2.13.1",
    "turbo": "^2.3.4",
    "typescript": "^5.7.3"
  },
  "simple-git-hooks": {
    "pre-commit": "pnpm type-check && pnpm lint && pnpm lint:fix"
  }
}
