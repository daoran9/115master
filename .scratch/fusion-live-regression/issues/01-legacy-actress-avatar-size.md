# 旧版页面女优头像尺寸复核

Status: resolved

## 现象

旧版 115 文件列表里的女优头像曾被放大，覆盖或撑高多行文件列表。

## 当前状态

- `2.0.0-beta.62` 已修复。
- `2.0.0-beta.84` 将新版头像移到原生文件名左侧，Fusion 附加区只保留增强操作。
- 自动化覆盖旧版列表、新版列表、刷新、React 重绘和目录切换。
- 新旧页面真实验收均确认头像为 `50x50`，新版头像紧邻文件名且不在附加区。

## 人工验收

1. 在真实旧版 115 页面打开包含女优资料的视频目录。
2. 打开番号详情。
3. 确认头像只出现在 Fusion 详情附加区。
4. 确认头像不覆盖原生文件行，也不撑高相邻文件行。

## 证据

- 原始截图：`D:\Temp\codex-clipboard-3aab0012-cf49-4ae5-ab4e-ce6e21b444ba.png`

## Answer

- beta.84 新版根目录：2 个演员头像，均为 `50x50`，选择器 `[data-115master-actress] + .file-name-responsive` 命中 2 个，附加区头像数量为 0。
- 刷新新版页面、进入 `KV` 子目录并返回根目录后，头像仍保持上述位置和尺寸。
- beta.84 旧版根目录：2 个头像，均为 `50x50`，仍挂在旧版 `.file-name-wrap` 文件行区域。
- 验证通过：根级单测、类型检查、lint、完整业务 E2E `101/101`。
